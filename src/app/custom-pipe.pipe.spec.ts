import { KebabPipePipe } from './custom-pipe.pipe';

describe('KebabPipePipe', () => {
  it('create an instance', () => {
    const pipe = new KebabPipePipe();
    expect(pipe).toBeTruthy();
  });
});
